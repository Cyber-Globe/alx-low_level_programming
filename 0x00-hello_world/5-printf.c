#include <stdio.h>
/**
 * main - print a line of c using printf function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        puts("with proper grammar, but the outcome is a piece of art,\n");
        return (0);
}

